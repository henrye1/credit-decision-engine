from .initialization import initialize_ped
# initialize_ped() <- I think this should be explicit for the user as it takes some time. 
# We can maybe do some detection in the flow to determine if its been initialized already.
